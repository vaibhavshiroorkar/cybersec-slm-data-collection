# Dashboard theme. Only the accent and font are set so a viewer's light/dark
# choice is preserved; card surfaces are drawn with theme-neutral rgba in
# dashboard/ui.py inject_css().
[theme]
primaryColor = "#4C6EF5"
font = "sans serif"
