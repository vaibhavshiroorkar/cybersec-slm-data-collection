# Environment
.env
.venv
venv
raw_data
cleaned_data

# Python
__pycache__
*.pyc
*.pyo
.pytest_cache
.ruff_cache
*.egg-info
build/
dist/

# Pipeline data (generated, too large for git)
# (READMEs inside these are force-added so the folders survive a clone)
raw_data/

# Source tracking belongs in sources/, not raw_data/
# Move Sources.xlsx -> sources/Sources.xlsx
*.xlsx
*.xls
cleaned/
clean_data/
flagged/
dropped/
_stages/

# fastText language-id model (downloaded locally, or set FASTTEXT_LID_MODEL)
lid.176.ftz
lid.176.bin

# Logs
logs/

# OS
.DS_Store
Thumbs.db

# uv
.python-version
